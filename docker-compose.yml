services:
  syncscreen:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "3001:3001"
      - "3002:3002"
    environment:
      - PORT=3001
      - PANEL_PORT=3002
    volumes:
      - ./server/data:/app/data
      - ./server/uploads:/app/uploads
    restart: unless-stopped
