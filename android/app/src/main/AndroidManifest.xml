<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <!-- false: permite instalação em set-top boxes genéricos que não reportam Leanback -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:banner="@mipmap/logomm"
        android:icon="@mipmap/logomm"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SyncScreen"
        android:usesCleartextTraffic="true">

        <!-- Activity principal: roteador invisível -->
        <activity
            android:name=".MainActivity"
            android:banner="@mipmap/logomm"
            android:exported="true"
            android:icon="@mipmap/logomm"
            android:label="@string/app_name"
            android:logo="@mipmap/logomm"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- LEANBACK_LAUNCHER: Android TV nativo -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <!-- LAUNCHER: set-top boxes genéricos, tablets em modo TV, Fire Stick -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SlideshowActivity"
            android:screenOrientation="landscape"
            android:launchMode="singleTask" />

        <activity
            android:name=".WebViewActivity"
            android:screenOrientation="landscape"
            android:launchMode="singleTask" />

        <!-- Inicia o app automaticamente após o boot da TV -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
